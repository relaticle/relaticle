# Custom Fields 4.0, Plan 5.2: Release

> **For agentic workers:** REQUIRED SUB-SKILL: sdd-lean, task-by-task. Runs LAST, after Plan 5.1 is green on the Relaticle side against `4.0.0-beta.1` (or a later beta). Spans both repos. Nothing here merges to main or tags without explicit instruction from the maintainer; every outward-facing text (changelog, announcement, issue follow-ups) is shown as a draft first. Spec: 2026-09-03-cf4-p5-host-adoption-and-release-spec.md.

**Goal:** Ship 4.0.0 with an upgrade path a 3.x host can follow without reading the source, and close the loops opened in 2025.

### Task 1: Upgrade guide and docs

- Upgrade guide: prerequisite (latest 3.x first), the `custom-fields:upgrade` run (record-links step, the ValidateSchemaStep gate, the purge step after verification), the published-migration edit for ULID/UUID hosts, every breaking change from the phase specs (interface signatures, contract renames, `lookup_type` retirement, settings collapse, config key renames, removed tooling, flag-default changes), and the MySQL enforcement caveat.
- Docs pages: relationships (definitions, cardinality, symmetric, history), option categories, through-relation tables (with the eager-load recipe), bulk paste, flavor registry, extension points (from Plan 1.1).
- Verify: docs build green; every breaking item in the guide traces to a spec section.

### Task 2: Changelog and licensing

- Changelog under 4.0.0, grouped: relationships, option categories, table surfaces, editor, modernization, breaking changes.
- The commercial license gains the field-of-use clause (phase 5 spec, 5.2); 3.x terms stay as sold. The clause is announced with the release, not buried in the changelog.
- Verify: maintainer approves both texts before anything is committed.

### Task 3: Production rehearsal (Relaticle)

- Migration rehearsal against a copy of production data (1,573 fields): run `custom-fields:upgrade`, verify reads in both directions (set and clear) through panel and API, run the purge step, re-verify; then `custom-fields:backfill-categories --dry-run` and review the unmatched-option report before the real run.
- Business-review of the full loop on the production-shaped stack (Horizon, Redis queue, Reverb).
- Verify: rehearsal log attached to the Relaticle PR; every defect found is fixed and re-run before tagging.

### Task 4: Tag and bump

- Delete nothing, rebase nothing: `4.x` is the release branch. Tag `4.0.0` on it with `gh release create v4.0.0 --latest` (bare tags do not sync).
- Confirm Packagist picked up the tag (`repo.packagist.org/p2/relaticle/custom-fields.json`) before the Relaticle bump; Packagist has missed close pushes before.
- One Relaticle PR bumps the constraint to `^4.0`, removes the path repository, and carries Plans 5.1 and 2.2. Merge on explicit instruction.
- Verify: `composer show relaticle/custom-fields` on the merged Relaticle main reports 4.0.0.

### Task 5: Close the loops

- Follow-up on relaticle/relaticle#469 (paired fields promised there); close custom-fields#53 and relaticle/relaticle#556 from the shipping PRs; check every box on custom-fields#210 and close it.
- Relaticle release notes and the package announcement; drafts shown first.
- Verify: no open checkbox remains on #210.
